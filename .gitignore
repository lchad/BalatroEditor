# macOS files
.DS_Store

# Claude Code working directory
.claude/

# profile
data/profile.json

# Electron dev dependencies (not shipped in builds — electron-builder handles bundling)
node_modules/

# demo saves (regenerated on demand)
data/*.jkr
data/save.jkr

# Electron build outputs
dist/
*.blockmap
